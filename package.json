{
  "name": "uroclinic-dr-jovani",
  "version": "2.6.0-111x",
  "private": true,
  "type": "module",
  "engines": {"node": ">=22 <23"},
  "scripts": {
    "dev": "wrangler dev",
    "build": "node scripts/build.mjs",
    "check": "node --check worker/index.js && node --check worker/lib/highlevel.js && node --check worker/lib/security.js && node --check worker/lib/ledger.js && node --check app.js",
    "test": "node --test test/*.test.mjs",
    "audit:secrets": "node scripts/secret-scan.mjs",
    "audit:actions": "node scripts/check-actions-pinned.mjs",
    "verify": "npm run check && npm test && npm run build && npm run audit:secrets && npm run audit:actions && git diff --check",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {"wrangler": "4.40.0"}
}
